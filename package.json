{
  "name": "camera-calculate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "gen:samples": "node ./scripts/generate-samples.mjs",
    "dev": "npm run gen:samples && vite",
    "build": "npm run gen:samples && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "svelte": "^4.2.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.2",
    "svelte-preprocess": "^5.1.4",
    "typescript": "^5.4.0",
    "vite": "^5.3.0"
  }
} 